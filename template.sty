% 版面設定・レイアウト・日本語対応など
\usepackage[twoside,
    top=20truemm,
    bottom=20truemm,
    inner=30truemm,
    outer=20truemm]{geometry}      % ページの余白を設定
\usepackage[dvipdfmx]{graphicx}    % 画像の挿入（dvipdfmx用）
\usepackage{hyperref, pxjahyper}   % ハイパーリンク対応（日本語もOK）
\usepackage{subfiles} % 章ごとにファイルを分割して管理

% 数式関係
\usepackage{amsmath, amssymb} % 数式環境・数学記号
\usepackage{mathtools}        % amsmathの拡張（式番号制御など）
\usepackage{bm}               % 数式中の太字ベクトル
\usepackage{physics}          % 物理向けコマンド（braket, dv, pdvなど）
\usepackage{mathrsfs}         % 花文字フォント（物理の作用などで使用）
\usepackage{amsthm}           % 定理・補題などの環境定義
% \usepackage{tensor}         % 添字を上下に綺麗に配置（テンソル記法）
% \usepackage{cancel}         % 数式中の取り消し線

% 図表関連
\usepackage[subrefformat=parens]{subcaption} % 図を(a)(b)形式で並べる
\usepackage{float}                           % [hbp] オプションを有効化
% \usepackage{tikz}                          % 図をTeX内で描画
% \usepackage{tikz-feynhand}                 % フェインマン図を描画
% \usepackage{caption}                       % 図表のキャプション制御
% \usepackage{wrapfig}                       % 文章の回り込み
% \usepackage{tablefootnote}                 % 表中に注釈を入れる


% その他
% \usepackage[version=4]{mhchem} % 化学式や反応式を簡単に入力
